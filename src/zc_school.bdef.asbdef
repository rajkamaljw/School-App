projection;
strict ( 2 );

define behavior for zc_school //alias <alias_name>
use etag
{
  use create;
  use update;
  use delete;

  use association _teacher { create; }
}

define behavior for ZC_Teacher //alias <alias_name>
use etag
{
  use update;
  use delete;

  use association _school;
}